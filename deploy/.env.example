# RabbitMQ default admin (MQTT + Management UI + platform lab)
RABBITMQ_USER=cogitor
RABBITMQ_PASS=cogitor
RABBITMQ_VHOST=/

# Node-RED credential encryption secret
NODERED_CREDENTIAL_SECRET=cogitor-nodered-dev
